services:
  homer:
    image: b4bz/homer:latest
    container_name: homer
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - homer_assets:/www/assets
    environment:
      - INIT_ASSETS=1

volumes:
  homer_assets:
